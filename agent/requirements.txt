# WorkTrackSync Agent Requirements

# Core dependencies
requests>=2.28.0
psutil>=5.9.0

# Windows specific (install only on Windows)
pywin32>=227; sys_platform == "win32"

# Development and build
pyinstaller>=5.0.0
